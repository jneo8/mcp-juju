[project]
name = "mcp-juju-demo"
version = "0.0.0"
description = "Deploy, verify and clean a demo environment for the mcp-juju MCP server, driven with Jubilant"
requires-python = ">=3.10"
dependencies = [
    "jubilant>=1,<2",
    "mcp>=1.0",
]

[tool.uv]
package = false
